#compdef _arplocal arplocal

local -a ifaces=( $(ip addr list | awk -F': ' '/^[0-9]/ {print $2}') )

_arguments "*:network interface:( ${ifaces[2,-1]} )"

#_arp_local() {
#  local -a commands
#  commands=(
#      'lan:wired network device'
#      'wifi:wireless network device'
#  )
#
#  if (( CURRENT == 2 )); then
#    _describe -t commands 'commands' commands
#  fi
#
#  return 0
#}
